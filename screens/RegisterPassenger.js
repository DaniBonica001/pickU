import react from "react";
import { StyleSheet, Text, View } from "react-native";

const RegisterPassenger = () => {
  return (
    <View style={styles.root}>
      <Text>RegisterPassenger</Text>
    </View>
  );
};

export default RegisterPassenger;
